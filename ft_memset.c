#include "libft.h"
void* ft_memset(void *b, int c, size_t len)
{
    while(){};
    return b;
}
int main ()
{
    char b[] = "42 bangkok";
    printf("%s\n", b);
    memset (b,'8',5);
    printf("%s\n", b);
}